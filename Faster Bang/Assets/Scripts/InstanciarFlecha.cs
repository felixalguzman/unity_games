using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InstanciarFlecha : MonoBehaviour {
    public GameObject nuevaFlecha;
	// Use this for initialization
	void Start () {
        //Instantiate(nuevaFlecha, new Vector3(10, 0.54f), Quaternion.identity);
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
